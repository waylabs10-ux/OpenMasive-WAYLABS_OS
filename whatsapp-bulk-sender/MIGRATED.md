# ⚠️ Proyecto movido

**WhatsApp Bulk Sender** vive ahora en su propio repositorio:

## https://github.com/waylabs10-ux/whatsapp-bulk-sender

Clona y trabaja desde ahí:

```bash
git clone https://github.com/waylabs10-ux/whatsapp-bulk-sender.git
cd whatsapp-bulk-sender
npm install
cp .env.example .env
npm start
```

Esta carpeta dentro de `OpenMasive-WAYLABS_OS` queda como referencia histórica.
